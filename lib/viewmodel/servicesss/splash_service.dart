import 'dart:async';
import 'package:get/get.dart';
import 'package:mvvmgetx/res/routes/routes_name.dart';
import '../controller/user_preference/user_prefrence_viewmodel.dart';

class SplashService {
  // void checkLoginStatus() {
  //   userPreferenceViewModel.getUser().then((user) {
  //     if (user!.token != null && user.token!.isNotEmpty) {
  //       Timer(Duration(seconds: 3), () => Get.toNamed(RoutesName.homeView));
  //     } else {
  //       Timer(Duration(seconds: 3), () => Get.toNamed(RoutesName.loginView));
  //     }
  //   });
  // }
}
